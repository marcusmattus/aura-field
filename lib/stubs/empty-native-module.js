/**
 * Metro stub for Expo Go / web when EXPO_PLATFORM !== 'native'.
 * Native-only packages (mmkv, nitro, maps) resolve here so bundling
 * never fails on missing peer native modules.
 */
module.exports = {};
